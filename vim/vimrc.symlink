" Setup pathogen
execute pathogen#infect()
syntax enable
" Setup color scheme to match terminal
set background=dark
colorscheme solarized
" Wrap gitcommit file types at the appropriate length
filetype indent plugin on

set number
